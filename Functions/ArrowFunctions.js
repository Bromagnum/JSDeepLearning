// regular func

// function add(a, b) {
//   return a + b;
// }
// arrow func

const add1 = (a, b) => {
  return a + b;
};
// Implicit return
const subtract1 = (a, b) => a - b;

// tek paramentre varsa

const double = (a) => a * 2;

// returning an object

const createObj = () => ({
  name: "brad",
});

// foreach
const numbers = [1, 2, 3, 4, 5];

// numbers.forEach(function (n) {
//   console.log(n);
// });

// Arrow func in a callback
// numbers.forEach(n => console.log(n))


// console.log(add1(1, 2));

// console.log(subtract1(5, 3));

// console.log(double(2));
